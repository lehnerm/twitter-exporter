postgres_test {
  profile = "slick.jdbc.PostgresProfile$"
  db {
    connectionPool = disabled
    driver = "org.postgresql.Driver"
    url = "jdbc:postgresql://localhost:5432/twitter_test"
    user = postgres
    password = example
  }
}
